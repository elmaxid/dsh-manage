diff --git a/index.js b/index.js
index d2fb688905291f8fae7071507d53c721bc1fe404..8b11a01e978c0128e4204516314404a00164fa47 100644
--- a/index.js
+++ b/index.js
@@ -80,8 +80,8 @@ export function apply(ctx) {
     if (hits.length === 0) return next()
     const list = [...new Set(hits)].join(', ')
     throw new Error(
-      `[secret-guard] 检测到疑似敏感信息（${list}），已阻止该命令，防止密钥泄漏进日志。` +
-      '请改用环境变量或文件传递密钥，不要把密钥写进命令行。'
+      `[secret-guard] Suspected sensitive data detected (${list}); command blocked to prevent a secret leaking into logs. ` +
+      'Use an environment variable or a file to pass secrets instead of writing them on the command line.'
     )
   }, { prepend: true })
 }
